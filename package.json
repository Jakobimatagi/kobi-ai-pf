{
  "name": "kobi-ai-pf",
  "version": "0.1.0",
  "private": true,
  "description": "AI-driven portfolio — micro-frontend architecture (Vue shell + Angular & React remotes)",
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=20.10.0"
  },
  "scripts": {
    "dev": "concurrently -n shell,weather,wordle -c blue,cyan,magenta \"pnpm dev:shell\" \"pnpm serve:weather\" \"pnpm dev:wordle\"",
    "build": "pnpm -r run build",
    "dev:shell": "pnpm --filter @kobi/shell run dev",
    "dev:weather": "pnpm --filter @kobi/mfe-weather run dev",
    "serve:weather": "pnpm --filter @kobi/mfe-weather run mf:serve",
    "dev:wordle": "pnpm --filter @kobi/mfe-wordle run start"
  },
  "devDependencies": {
    "concurrently": "^10.0.4"
  }
}
