{
  "name": "kobi-ai-pf",
  "version": "0.1.0",
  "private": true,
  "description": "AI-driven portfolio — micro-frontend architecture (Vue shell + Angular & React remotes)",
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=20.10.0"
  },
  "scripts": {
    "dev": "concurrently -n shell,react,angular -c blue,cyan,magenta \"pnpm dev:shell\" \"pnpm serve:react\" \"pnpm dev:angular\"",
    "build": "pnpm -r run build",
    "dev:shell": "pnpm --filter @kobi/shell run dev",
    "dev:react": "pnpm --filter @kobi/mfe-react run dev",
    "serve:react": "pnpm --filter @kobi/mfe-react run mf:serve",
    "dev:angular": "pnpm --filter @kobi/mfe-angular run start"
  },
  "devDependencies": {
    "concurrently": "^10.0.4"
  }
}
